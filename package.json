{
  "name": "team-pjt",
  "version": "0.1.0",
  "description": "standalonemaster",
  "engines": {
    "npm": "9.6.4",
    "node": "v18.14.0",
    "nodemon": "2.0.21"
  },
  "scripts": {
    "client-build": "cd client && npm i && npm run build && cd ../",
    "server-build": "cd server && npm i && cd ../ ",
    "heroku-prebuild": "npm run client-build && npm run server-build",
    "start": "cd server && nodemon ./server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dbswp/2023_TEAM_siba"
  }
}
